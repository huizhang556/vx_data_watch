# 版本功能

本文件记录 VX Data Watch 各正式版本的主要功能。版本以 [GitHub 主仓库](https://github.com/huizhang556/vx_data_watch/tags) 为准；中国大陆访问可使用 [Gitee 同步仓库](https://gitee.com/huizhang556/vx_data_watch)。

## 0.5.2 - 2026-09-01

### 下载队列与文件保存

- 下载内容页拆分为“下载队列”和“完成队列”两个同级 Tab；下载队列展示未完成任务，完成队列展示已完成任务，并显示各自的任务数量。
- 下载队列表格的进度区域增加单任务暂停、继续、取消和删除图标，用户不必通过批量勾选即可控制单个任务。
- 完成队列中的每条任务提供“保存至本地”快捷图标；浏览器支持目录授权时写入用户选择的文件夹，不支持时回退到浏览器默认下载目录。
- 下载操作区明确说明批量开始、暂停、继续、取消和删除仅作用于当前勾选任务；“清空列表”仅清理已完成、失败或已取消任务。
- 下载配置页分开显示服务器保存路径和本地保存位置，并说明服务器用于执行/暂存任务，本地用于取回完成文件。

### 主题系统

- 按当前产品保留的晨曦、玫瑰柔和、薰衣草、雾蓝、薄荷和奶油六套主题，统一补充背景、卡片、文字、边框、按钮、链接、悬停、成功、警告、错误和信息状态变量。
- 新增下载队列、完成队列、AI 配置、AI 聊天、表格、弹窗和更新区域统一使用语义 CSS Variables，移除内容区域的强渐变背景，改善不同主题下的对比度。
- 暮夜主题不再出现在主题选择器；历史 `dark`/`night` 值自动回退到晨曦模式，主题选择继续保存到 `localStorage`，保留跟随系统选项。

### 一键安装与数据库安全

- 一键安装按公网地区设置默认代码源和镜像源：中国大陆默认 Gitee + 阿里云 ACR，非中国大陆默认 GitHub + Docker Hub；用户仍可手动切换，首选代码源失败时自动回退。
- 一键安装默认选择 SQLite，同时保留 PostgreSQL 手动选择；已有 `.env` 时沿用原数据库和镜像配置。
- PostgreSQL 备份与恢复改用 `PGPASSWORD` 环境变量传递密码，避免凭据出现在进程命令行参数中。

### 测试与文档

- 增加 SQLite 加密备份恢复、PostgreSQL 客户端调用契约、模型缓存/快捷配置约束及完成下载文件打包的回归测试。
- 更新 README 和本地发布手册，补充六套主题、双数据库、双代码源、双镜像源以及下载队列和本地保存行为说明。
- 统一 Python 包、前端包、Docker 默认构建参数和应用运行时版本为 `0.5.2`。

## 0.5.1 - 2026-08-31

### 0.5.1 发布后功能补充

- 下载内容页拆分为“下载队列”和“完成队列”两个同级视图；未完成任务与已完成任务分别展示，切换队列时自动清除勾选，避免跨队列误操作。
- 下载列表的每条任务增加独立的暂停、继续、取消和删除图标；完成任务增加“保存至本地”图标，可将服务器已完成文件取回到浏览器授权的本地目录。
- 下载操作区明确区分批量操作与清理操作：开始、暂停、继续、取消和删除选中只处理已勾选任务，“清空列表”清理所有已完成、失败或已取消任务。
- 下载配置页明确显示服务器保存路径和本地保存位置，并分别说明服务器端执行/暂存与本地取回的用途；不支持目录授权的浏览器自动使用默认下载目录。
- 一键安装按公网地区设置默认源：中国大陆默认 Gitee 和阿里云 ACR，非中国大陆默认 GitHub 和 Docker Hub；用户仍可手动切换，首选代码源失败时自动回退。
- 一键安装默认选择 SQLite，保留 PostgreSQL 手动选择；已有 `.env` 时所有地区均沿用既有数据库和镜像配置。
- 增加 SQLite 加密备份恢复、PostgreSQL `pg_dump/psql` 调用契约、完成下载文件打包及模型缓存/快捷配置约束的自动化回归测试。
- PostgreSQL 备份与恢复不再将数据库密码放入命令行参数，改用 `PGPASSWORD` 环境变量传递，降低进程列表泄露凭据的风险。

### 数据库与运维

- 一键安装新增 SQLite/PostgreSQL 选择，PostgreSQL 自动创建内置数据库容器、数据卷和随机凭据。
- 更新、本机备份和异地迁移按数据库类型处理；PostgreSQL 额外生成 `pg_dump` 转储文件。
- PostgreSQL 加密备份支持通过 `pg_dump` 创建、通过 `psql` 恢复。
- Docker Compose 增加 PostgreSQL profile 和独立的 `vx-postgres` 数据卷。
- Docker 镜像内置 PostgreSQL 客户端工具，支持备份和恢复操作。
- AI 接口配置改为管理员全局配置，新建配置不再绑定当前视频号账号；所有用户在授权范围内共享管理员发布的接口列表。
- 保留旧版本账号级配置的兼容读取；管理员编辑旧配置后会自动提升为全局配置，避免升级后丢失已有接口。

### 数据库迁移稳定性修复

- 整理 `0.5.1` 新增迁移的父子关系，移除重复 revision ID 和循环依赖，确保 SQLite 与 PostgreSQL 都能按单一迁移链升级。
- 为视频号账号归属和聊天分类配置的外键增加明确约束名称，修复 SQLite 批量表变更时 `Constraint must have a name` 导致应用反复重启的问题。
- 更新启动流程验证 Alembic head 和数据库迁移结果；迁移失败时容器会保持失败状态并输出明确日志，避免错误地报告服务已就绪。
- 补充在线更新版本检测测试，确保当前版本不会被错误列为可更新版本，并覆盖更新排队和并发更新保护。

### 主题与文档

- 统一七套主题的语义 CSS Variables，覆盖背景、卡片、文字、边框、按钮、链接及状态色。
- 优化登录、表单、表格、弹窗、AI 速问、下载和在线更新区域在不同主题下的对比度与悬停状态。
- 修复暮夜模式使用旧 `dark` 样式键导致部分页面回落到浅色配色的问题，为 `night` 补齐完整语义变量和组件状态色。
- 重写 README 的项目简介、主要功能、双数据库、双代码源、双镜像源和一键运维说明。
- 修正 Gitee 脚本直链和 Docker Compose 部署示例的代码块与操作顺序。
- 一键安装文档明确区分临时下载目录和正式安装目录；正式项目固定安装到 `/opt/vx-data-watch`，临时目录使用 `/tmp/vx-data-install`。

### AI 配置与快速配置界面

- 管理员 AI 配置页独立显示已保存的全局接口记录，不再混入 AI 建议页的数据完整性提示或重复配置入口。
- 全局配置列表显示配置名称、默认模型、Base URL、协议和当前启用状态，并支持直接编辑或新增配置。
- AI 配置弹窗标题随“官方接口”和“OPENAI 兼容”Tab 联动，同时显示“新增配置”或“编辑旧配置”操作状态。
- 合并 AI 建议页面重复的快速配置区域，统一显示创建日期、关联分析数量、编辑、应用和删除操作；每个用户最多保存 5 项。

### AI 聊天侧栏与搜索交互补充

- AI 聊天侧栏在搜索框为空时显示分类树；输入关键词后自动切换为按对话名称匹配的扁平结果列表，并显示所属分类。
- 点击搜索结果会恢复对应会话历史并继续对话；清空搜索框后自动返回分类树视图。
- 搜索结果保留编辑、置顶/取消置顶和删除操作，且与分类树节点使用一致的权限和数据隔离规则。
- 分类节点和对话节点禁用浏览器右键菜单及双击操作；通过单击节流避免双击间接触发展开、收缩或打开会话。
- 收缩侧栏时仅对包含多个子项的菜单显示悬浮二级菜单，单子项菜单直接进入对应页面；统一修复暮夜模式悬浮菜单的背景、文字和选中对比度。
- AI 聊天厂商和模型下拉框调整为紧凑固定宽度，移动端自动恢复弹性布局。

### 主题适配与发布验证

- 为侧栏、移动导航、AI 聊天侧栏、消息气泡、搜索结果和菜单状态补充七套主题语义变量覆盖，避免旧版固定颜色覆盖主题配置。
- 修复新增 `0.5.1` 迁移未接入主链导致的 Alembic 多 head 问题，确保启动时按单一迁移链升级。
- 使用 Python 3.12 容器完成后端 28 项测试，完成前端 Vitest、TypeScript 和 ESLint 检查。
- 使用 `--provenance=false --sbom=false` 构建兼容版 `0.5.1` 镜像，并同步 `latest` 标签。

### 主题调整

- 根据产品要求移除“暮夜模式”选项，主题选择器现在提供晨曦、玫瑰柔和、薰衣草、雾蓝、薄荷和奶油六套主题。
- 保留 `localStorage` 持久化；历史保存的 `night`/`dark` 值会自动回退到晨曦模式，跟随系统时也使用晨曦模式。

## 0.5.0 - 2026-08-31

### 发布补充
- 在线更新检测失败时保留已有版本库数据，并提供重新检测、刷新页面和切换镜像源的恢复入口；检测失败期间禁止误触发版本切换。
- Docker Compose 未指定 `VX_IMAGE` 时默认使用 `docker.io/litehub/vx-data-watch:latest`，避免误启动旧的 `vx-data:local` 镜像。
- 下载配置页进入时显示服务器出口 IP、地区检测中的状态及失败原因，帮助用户判断是否需要配置代理。

### AI 速问与模型接口

- AI 速问支持 Anthropic Messages 原生协议，包括系统提示词、消息请求和流式文本响应。
- AI 速问支持 Google Gemini 原生 `generateContent` 和 `streamGenerateContent` 接口。
- AI 速问支持 Grok/xAI 官方 Chat Completions 接口，并提供独立协议选项。
- 模型查询接口支持 OpenAI、Responses、Anthropic、Gemini 和 Grok/xAI 配置。
- AI 配置页面增加 Anthropic Messages、Gemini 和 Grok 协议选项。
- 保留 OpenAI Chat Completions、Responses 及其他 OpenAI 兼容服务配置能力。

### 会话、附件与历史记录

- 增加 AI 速问会话创建、恢复、重命名、置顶和删除。
- 增加会话搜索、批量选择和批量删除。
- 增加分类创建、重命名、删除和拖拽排序，并持久化分类顺序。
- 支持 Markdown 和 JSON 格式导出聊天记录。
- 支持文本、Markdown、CSV、JSON 和图片附件。
- 支持从剪贴板直接粘贴图片到聊天输入区。
- 限制单个附件 10 MB、单次附件总量 32 MB，并校验附件类型。
- 历史文本附件支持下载，历史图片附件支持缩略图预览和点击放大。
- 附件按会话目录保存，删除会话时同步清理附件文件。
- 增加附件安全路径校验，防止越权访问其他用户文件。

### 用户等级与权限

- 增加普通用户、会员用户、超级会员和管理员四级用户等级。
- 增加 AI 速问、数据分析和视频下载的每日使用次数限制。
- 普通用户每日可使用 AI 速问 20 次、数据分析 5 次、下载 5 个视频。
- 会员用户每日可使用 AI 速问 50 次、数据分析 10 次、下载 20 个视频。
- 超级会员每日可使用 AI 速问 100 次、数据分析 20 次、下载 50 个视频。
- 管理员不受上述每日额度限制。
- 新注册用户默认设置为普通用户等级。
- 增加每日用量统计表和对应数据库迁移。
- 修复普通用户被错误判定为未开通 AI 速问的问题。

### 多主题界面

- 新增晨曦模式，作为原白天模式的正式名称。
- 新增暮夜模式，作为原黑夜模式的正式名称。
- 保留玫瑰柔和模式。
- 新增薰衣草、雾蓝、薄荷和奶油四套低饱和度主题。
- 所有主题统一使用 CSS Variables 管理背景、卡片、文字、边框、按钮、链接和状态色。
- 统一成功、警告和错误提示的主题适配颜色。
- 用户选择的主题保存到浏览器 `localStorage`。
- 未选择主题时默认跟随系统明暗设置，系统主题变化时自动同步。
- 兼容旧版本保存的 `light` 和 `dark` 主题值。

### 测试与工程修复

- 增加 Anthropic、Gemini 和 Grok 原生协议的本地模拟测试。
- 增加 AI 速问接口级端到端测试，覆盖会话、消息、附件、流式响应、历史读取、额度和删除清理。
- 修复聊天附件处理缺少 `uuid` 导入的问题。
- 修复聊天附件模型未正确导入导致附件消息发送失败的问题。
- 完成前端 lint、生产构建、后端测试和 Python 编译验证。

## 0.4.3 - 2026-08-28

- 新增一键运维脚本，支持 Ubuntu 24.04 和 Debian 12 的安装、更新、备份迁移和卸载。
- 一键安装显示公网 IP、国家代码和镜像建议，支持用户选择 Docker Hub 或阿里云 ACR，并处理网络重试。
- 更新脚本增加更新前备份、健康检查和失败回滚；迁移使用 rsync 断点续传并支持选择是否包含大文件。
- Docker Compose 默认宿主机端口调整为 `10000`，容器内部端口固定为 `8000`。
- 在线更新增加 Docker Hub/阿里云 ACR 受信任镜像源选择和完整仓库地址显示。
- 优化黑夜模式下描述列表、表格、表单、提示框和正文的文字对比度及悬停状态。
- 补充部署文档中的必需配置、可选配置、镜像源选择、Nginx 指引和卸载说明。

## 0.4.2 - 2026-08-27

- 完善视频下载功能，支持任务暂停、继续、取消、代理测试和启动时自动检测代理。
- 下载完成后补充视频标题、时长和预计文件大小等任务信息。
- 顶部用户头像使用已保存的头像图片。
- 规范 Docker Compose 容器名称为 `vx-data-watch-app` 和 `vx-data-watch-updater`。

## 0.4.1 - 2026-08-23

- 完善 YouTube 下载配置，支持视频质量、下载类型、高清封面、H.264 + AAC 兼容性转码、转码质量和保留原文件设置。
- 新增 yt-dlp 下载引擎、ffmpeg 运行环境和持久化下载队列，支持批量添加链接、进度、状态、开始、取消、删除和清空任务。
- Cookies 支持用户手动粘贴 Netscape 格式文本，保存时加密存储，并在下载前提供格式检测。
- 优化浅色和深色主题的整体 UI，包括侧边栏、收起状态二级菜单、顶部用户菜单、卡片、表格、表单和移动端导航。
- 侧边栏一级菜单改为互斥展开，数据概览补充图标，用户下拉菜单固定宽度。
- 本地用户支持默认头像、头像展示、新增用户上传头像和编辑用户更换头像；个人资料页展示邮箱、角色、状态、注册时间和最近登录时间。
- 统一应用、Python 包、前端包和 Docker 镜像版本为 `0.4.1`。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.4.1`。

## 0.4.0 - 2026-08-21

- 重组侧边栏，将数据分析拆分为数据概览、视频贡献、数据导入和 AI 建议二级菜单。
- 增加视频下载一级菜单，包含下载配置和下载内容页面。
- 增加全局浅色/深色主题切换，并优化认证页和主页界面。
- 增加用户头像菜单、个人资料页面和头像上传展示。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.4.0`。

## 0.3.9 - 2026-08-21

- 修复登录页用户名和密码输入框与表单状态不同步的问题，兼容浏览器自动填充和密码管理器填充。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.9`。

## 0.3.8 - 2026-08-21

- 将加密备份从系统设置中独立为侧边栏功能，支持创建、查看和下载备份。
- 本地用户新增注册邮箱字段，保留表格行级编辑和删除操作，移除顶部重复的用户名和密码修改入口。
- 邮箱注册和人机验证配置调整为清晰的两列布局，开关改为圆角方形控件，并收窄相关下拉框。
- 为用户名、邮箱、密码和验证码增加前后端格式校验，并提供中文错误提示。
- 新增独立的加密备份页面和移动端导航入口。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.8`。

## 0.3.7 - 2026-08-21

- 新增可配置的邮箱注册、邮箱验证码登录和密码重置功能。
- 新增 SMTP 邮件发送配置，支持 587 STARTTLS 和 465 SSL。
- 新增 Cloudflare Turnstile 人机验证，可用于登录、注册和重置密码。
- 新增用户名修改功能，统一处理退出登录和会话失效后的登录页跳转。
- 恢复高分辨率截图 OCR 缩放处理，降低识别内存峰值。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.7`。

## 0.3.6 - 2026-08-18

- 统一数据概览、视频贡献和 AI 建议的日期选择规则，增加自定义日期区间。
- 新增按账号查询可用数据日期，禁止选择没有数据或超过今天的日期，并置灰缺少数据的快捷区间。
- 部署文档补充 OCR 最低 CPU、内存和 swap 要求。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.6`。

## 0.3.5 - 2026-08-18

- 修复在线更新共享目录权限，更新器启动时自动为应用用户准备 `updates` 目录。
- 优化高分辨率截图 OCR，限制识别工作尺寸并主动回收图片处理内存，降低小型服务器 OOM 风险。
- 数据概览、视频贡献和 AI 建议统一支持自定义日期区间，并禁止选择未来日期或数据库没有记录的日期。
- 部署文档补充 OCR 的最低 CPU、内存和 swap 建议。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.5`。

## 0.3.4 - 2026-08-18

- 修复在线更新版本检测重复请求和 Docker Hub 网络抖动导致的错误提示。
- 概览、视频贡献和 AI 建议统一支持开始日期、结束日期及数据完整性提示。
- 增加 AI 接口配置删除能力，改进截图 OCR 网关错误提示。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.4`。

## 0.3.3 - 2026-08-18

- 数据概览和 AI 建议增加时间段数据完整性检查，数据不足时给出明确提示。
- AI 报告支持在数据概览中按查询范围匹配并直接查看历史报告。
- 修复 OCR 图像输入兼容性，支持微信官方视频明细 CSV 字段并改进数字识别。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.3`。

## 0.3.2 - 2026-08-18

- 完善 AI 配置缓存、历史报告查看和任意历史日期导入。
- 发布 Docker 镜像 `litehub/vx-data-watch:0.3.2`。
- 在线更新移至左侧独立菜单，默认监听地址改为 `127.0.0.1`，便于通过 Nginx 反向代理。
- 截图 OCR 上传失败时显示具体文件和大小限制原因，并对 413 请求提供分批上传提示。
- 在线更新版本列表显示镜像库中的全部正式版本，支持升级和回退到历史版本。

## 0.3.1 - 2026-08-18

- 修复 HTTPS 配置与 HTTP 访问组合下登录 Cookie 未被浏览器保存，导致后续操作提示“请先登录”的问题。
- 登录后增加服务端会话确认，避免界面显示已登录但实际会话不存在。
- OpenAI 兼容接口自动兼容服务商根地址和 `/v1`，并支持按视频号账号保存、切换多个接口配置。
- 分析报告保存正文、Prompt、数据快照和配置快照，查看历史时直接读取缓存。
- 查询记录改为只读查看和删除，修改日期需要重新生成新的分析记录。
- 每日 CSV、视频表格和截图导入支持任意数据日期，CSV 校验用户选择的数据截止日期。
- 趋势游标保留涨跌箭头和比例，移除“上升/下降/持平”文字。

## 0.3.0 - 2026-08-18

- 增加 Docker Hub 正式版本检测、历史版本选择、升级前备份、在线更新和自动重启。
- 使用隔离的 Companion 更新服务，主 Web 容器不直接访问 Docker Socket。
- 更新失败时恢复原容器，成功后持久化镜像版本并自动刷新页面。
- Compose 文件统一为 `docker-compose.yaml`，完善公开部署说明。
- 官方镜像发布到 `litehub/vx-data-watch`，Linux 不再提供单文件二进制。

## 0.2.1 - 2026-08-18

- AI 查询记录支持重新生成并查看分析、编辑日期范围和确认删除。
- 查询记录操作写入审计日志，查看历史不会创建重复记录。
- 侧边栏展开/收缩按钮调整到右侧边缘垂直中点。
- 增加持续集成、公开部署说明和隐私文件排除规则。

## 0.2.0 - 2026-08-17

- 概览支持单日、近 3/7/15/30 日和自定义区间。
- 趋势覆盖播放、点赞、评论、分享、关注、转发和收藏，并显示同期涨跌。
- 增加趋势、互动构成和视频贡献图表。
- AI 建议支持区间分析、Markdown 富文本、图表和查询记录。
- AI 配置支持获取模型、独立测试和保存。
- 修复 OCR 错位和误识别、退出页面不跳转、AI 报告不显示及按钮联动问题。
- 支持 Docker Compose、本地源码部署、加密配置、审计和备份。

## 0.1.0 - 2026-08-17

- 完成账号管理、7 日 CSV、逐视频表格、截图 OCR、数据概览和 AI 接口配置的首个可运行版本。
